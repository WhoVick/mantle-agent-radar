{
  "name": "mantle-agent-radar",
  "version": "0.1.0",
  "private": true,
  "description": "AI Alpha & Data agent for Mantle investor-grade signals.",
  "scripts": {
    "build": "python -m agent_radar.build_signals",
    "build:deploy": "node tools/build_deploy_page.mjs",
    "check": "python -B -c \"import agent_radar.models, agent_radar.scoring, agent_radar.build_signals\" && node --check web/app.js",
    "preview": "python -m http.server 8899 --bind 127.0.0.1 --directory web",
    "hash:top": "python tools/hash_signal.py --top"
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "solc": "^0.8.35"
  }
}
